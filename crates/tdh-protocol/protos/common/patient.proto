syntax = "proto3";

package tdh.protocol.common;

// PatientIdentity - patient identifier information
message PatientIdentity {
  int32 account_id = 1;
  int32 user_profile_id = 2;
  int32 tenant_id = 3;
  string oidc_user_id = 4;  // Optional field
}
